# polyfont-themes

Theme import, export, and built-in theme registry for polyfont.

Provides `ThemeImporter` (VSCode JSON, TextMate tmTheme), `ThemeExporter` (polyfont TOML, VSCode settings), and `ThemeRegistry` with 5 built-in themes.
